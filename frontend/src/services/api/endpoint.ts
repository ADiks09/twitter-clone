const USER = {
  REGISTER: '/api/auth/register',
  LOGIN: '/api/auth/login',
}

const POST = {
  CREATE: '/api/post/create',
  GET_COLLECTION_BY_USER_NAME: '/api/post/postsByUserName',
}

const PROFILE = {
  USER: 'api/profile/user',
}

export const API = {
  USER,
  PROFILE,
  POST,
}
